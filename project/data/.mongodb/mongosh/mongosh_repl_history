exit
db.carpositions.delete({})
use car-position
)
  }
    ]
        { role: "read", db: "local" }
        { role: "clusterMonitor", db: "admin" },
    roles: [
    pwd: "password",
    user: "exporter",
  {
db.createUser(
use admin